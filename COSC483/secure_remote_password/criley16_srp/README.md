# Requirements:
 - hashlib (should come with python now but if not: `pip install hashlib` )



# Run:
Note: the value 'a' was randomly generated once and then kept constant as that value for consistency, if wanted to have a be random comment out line 120

**Default:**


`python ./srp.py`

---
**Interactive Mode** (will require pass off website for inputting values)

`python ./srp.py -i`


